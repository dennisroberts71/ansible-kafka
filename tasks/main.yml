---
- include: epel.yml

- include: java.yml

- include: scala.yml

- include: zookeeper.yml

- include: apache-download.yml
  vars:
    url_path: "{{ kafka_url_path }}"
    file_name: "{{ kafka_tgz_file }}"
